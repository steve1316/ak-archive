/** Types for `keys.mjs`, so `src/lib/story.ts` can import the key rules under the app's strict typecheck. */

/** The published key of a sprite or story image. See `keys.mjs`. */
export declare function spriteKey(name: string): string;

/** The key a music track is titled by, the same for its intro and its loop. See `keys.mjs`. */
export declare function musicKey(ref: string): string;
