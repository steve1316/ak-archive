/** Types for `keys.mjs`, so `src/lib/story.ts` can import the sprite key rule under the app's strict typecheck. */

/** The published key of a sprite or story image. See `keys.mjs`. */
export declare function spriteKey(name: string): string;
